jenkins:
  systemMessage: "Jenkins is auto configured from Jenkins Configuration as Code plugin"
  
  securityRealm:
    local:
      allowsSignup: false
      users:
        - id: "admin"
          password: "s3cret"
  authorizationStrategy: loggedInUsersCanDoAnything
